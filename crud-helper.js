require('dotenv').config()
require('./config/database')

const User = require('./models/api/user')


//seed two users and two movies and two comments





let user, item, category, order
let users, items, categories, orders